namespace Payment.Domain.SeedWork
{
		// this is a marker interface class that should always remain empty
		// the purpose is to mark an entity class as an aggregate root
    public interface IAggregateRoot { }
}
